class TextAsBackground{constructor(x,y){
    this.x = x;
    this.y = y;}

HAI(){push();
    textSize(16);
    textFont('Comic Sans Ms');
    translate(this.x,this.y);

    fill(random(myColors));
    text('HOW ARE YOU,TODAY?', width*0, height*.01, width*1, height*.5,);
    fill(random(myColors));
    text('ARE YOU HAPPY?', width*0, height*.04, width*1, height*.5,);
    fill(random(myColors));
    text('IS EVERYTHING OKAY?', width*0, height*.07, width*1, height*.5,);

    fill(random(myColors));
    text('HOW ARE YOU, TODAY?', width*0, height*.1, width*1, height*.5,);
    fill(random(myColors));
    text('ARE YOU HAPPY?', width*0, height*.13, width*1, height*.5,);
    fill(random(myColors));
    text('IS EVERYTHING OKAY?', width*0, height*.16, width*1, height*.5,);

    fill(random(myColors));
    text('HOW ARE YOU, TODAY?', width*0, height*.19, width*1, height*.5,);
    fill(random(myColors));
    text('ARE YOU HAPPY?', width*0, height*.22, width*1, height*.5,);
    fill(random(myColors));
    text('IS EVERYTHING OKAY?', width*0, height*.25, width*1, height*.5,);

    fill(random(myColors));
    text('HOW ARE YOU, TODAY?', width*0, height*.28, width*1, height*.5,);
    fill(random(myColors));
    text('ARE YOU HAPPY?', width*0, height*.31, width*1, height*.5,);
    fill(random(myColors));
    text('IS EVERYTHING OKAY?', width*0, height*.34, width*1, height*.5,);

    fill(random(myColors));
    text('HOW ARE YOU, TODAY?', width*0, height*.37, width*1, height*.5,);
    fill(random(myColors));
    text('ARE YOU HAPPY?', width*0, height*.41, width*1, height*.5,);
    fill(random(myColors));
    text('IS EVERYTHING OKAY?', width*0, height*.44, width*1, height*.5,);

    fill(random(myColors));
    text('HOW ARE YOU, TODAY?', width*0, height*.47, width*1, height*.5,);
    fill(random(myColors));
    text('ARE YOU HAPPY?', width*0, height*.50, width*1, height*.5,);
    fill(random(myColors));
    text('IS EVERYTHING OKAY?', width*0, height*.53, width*1, height*.5,);

    fill(random(myColors));
    text('HOW ARE YOU, TODAY?', width*0, height*.56, width*1, height*.5,);
    fill(random(myColors));
    text('ARE YOU HAPPY?', width*0, height*.59, width*1, height*.5,);
    fill(random(myColors));
    text('IS EVERYTHING OKAY?', width*0, height*.62, width*1, height*.5,);

    fill(random(myColors));
    text('HOW ARE YOU, TODAY?', width*0, height*.65, width*1, height*.5,);
    fill(random(myColors));
    text('ARE YOU HAPPY?', width*0, height*.68, width*1, height*.5,);
    fill(random(myColors));
    text('IS EVERYTHING OKAY?', width*0, height*.71, width*1, height*.5,);

    fill(random(myColors));
    text('HOW ARE YOU, TODAY?', width*0, height*.74, width*1, height*.5,);
    fill(random(myColors));
    text('ARE YOU HAPPY?', width*0, height*.77, width*1, height*.5,);
    fill(random(myColors));
    text('IS EVERYTHING OKAY?', width*0, height*.8, width*1, height*.5,);

    fill(random(myColors));
    text('HOW ARE YOU, TODAY?', width*0, height*.83, width*1, height*.5,);
    fill(random(myColors));
    text('ARE YOU HAPPY?', width*0, height*.86, width*1, height*.5,);
    fill(random(myColors));
    text('IS EVERYTHING OKAY?', width*0, height*.89, width*1, height*.5,);

    fill(random(myColors));
    text('HOW ARE YOU, TODAY?', width*0, height*.92, width*1, height*.5,);
    fill(random(myColors));
    text('ARE YOU HAPPY?', width*0, height*.95, width*1, height*.5,);
    fill(random(myColors));
    text('IS EVERYTHING OKAY?', width*0, height*.98, width*1, height*.5,);
    pop();}}